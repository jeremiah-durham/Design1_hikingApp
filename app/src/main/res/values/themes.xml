<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/textPrimary</item>
    </style>

    <style name="Heading1" parent="TextAppearance.AppCompat.Large">
        <!-- Primary brand color. -->
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:fontFamily">@font/lato_semibold</item>
    </style>

    <style name="Heading2" parent="TextAppearance.AppCompat.Large">
        <!-- Primary brand color. -->
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:fontFamily">@font/lato_semibold</item>
    </style>

    <style name="Heading3" parent="TextAppearance.AppCompat.Large">
        <!-- Primary brand color. -->
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:fontFamily">@font/lato_semibold</item>
    </style>

    <style name="Subheading" parent="TextAppearance.AppCompat.Large">
        <!-- Primary brand color. -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:fontFamily">@font/lato_regular</item>
    </style>

    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <!-- Primary brand color. -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:fontFamily">@font/lato_regular</item>
    </style>
</resources>
